//  RDS Database
//  RDS Database Configuration
async function connectRDS() {
    // RDS Database connection code
    const AWS = require('aws-sdk');
    const rdsDataService = new AWS.RDSDataService();
}